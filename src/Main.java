import java.util.Arrays;
import java.util.Random;

public class Main {
    public static void main(String[] args) {
       NumerosPrimos numerosPrimos=new NumerosPrimos();
        System.out.println(Arrays.toString(numerosPrimos.numeros));
        System.out.println(Arrays.toString(numerosPrimos.numerosPrimos));
       //numerosPrimos.devolerNumerosPrimos();
        System.out.println(Arrays.toString(numerosPrimos.numeros));
        System.out.println(Arrays.toString(numerosPrimos.numerosPrimos));
    }




}